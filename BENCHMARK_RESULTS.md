# muduoSelf 网络库 QPS 压测报告

**日期**: 2026-05-08  
**环境**: WSL2 Linux, GCC 11.4, 本机回环 127.0.0.1:8000  
**测试工具**: `example/benchmark.cpp` (多线程 epoll 压测客户端)  
**被测服务**: `example/benchserver.cpp` (持久连接 Echo 服务器, 4 IO 线程)

---

## 测试方法

```
benchmark (压测端)                    benchserver (被压端)
─────────────────                    ────────────────
N 个 TCP 长连接 ──── 建立连接 ────→ Acceptor accept
每连接循环收发 64B 请求 ───────────→ 4 个 IO 线程调度
                              ────→ TcpConnection::onMessage (echo)
                              ←──── 64B 响应回客户端
客户端收到响应立即发下一个 ────────→ 循环 (pipeline=1)
```

- 每个连接维持 pipeline 深度 = 1（发一条 → 等待完整回包 → 再发）
- 连接预热 2 秒后开始采集，运行 10 秒
- 每 5 秒输出中间 QPS 以观察稳定性
- 延迟采集：分桶法，500us 粒度，无锁原子累加

---

## 综合结果

| # | 连接数 | 客户端线程 | 消息 | QPS (req/s) | 带宽 | avg 延迟 | p50 | p99 | p999 |
|---|--------|-----------|------|-------------|------|----------|-----|-----|------|
| 1 | 500 | 2 | 64B | **88,656** | 5.4 MB/s | 5.6ms | 5.8ms | 7.8ms | 11.8ms |
| 2 | 2000 | 4 | 64B | **100,248** | 6.1 MB/s | 20.3ms | 19.8ms | 25.3ms | 122ms |
| 3 | 4000 | 4 | 64B | 85,508 | 5.2 MB/s | 47.9ms | 46.8ms | 59.8ms | 805ms |
| 4 | 1000 | 4 | 256B | **99,464** | 24.3 MB/s | 10.2ms | 10.3ms | 12.8ms | 20.8ms |
| 5 | 500 | 4 | 1024B | 93,669 | **91.5 MB/s** | 5.3ms | 5.3ms | 7.3ms | 11.3ms |
| 6 | 4000 | 8 | 64B | 82,500 | 5.0 MB/s | 49.6ms | 47.8ms | 65.3ms | 765ms |

### 30 秒长运行 (2000 conns, 4 threads, 64B)

| 时间点 | 累计请求 | QPS |
|--------|----------|-----|
| 5s | 471,912 | 94,380 |
| 10s | 946,354 | 94,635 |
| 15s | 1,412,101 | 94,133 |
| 20s | 1,837,745 | 91,887 |
| 25s | 2,280,350 | 91,214 |
| 30s | 2,726,452 | **90,881** |

**最终**: 总 2,726,666 响应，30 秒稳态 QPS = **90,888**，延迟 avg=21.9ms p99=28.8ms

---

## 逐项分析

### 1. 延迟 vs 连接数

```
连接数      QPS      avg延迟    p99延迟
500  →    88.6K      5.6ms      7.8ms
2000 →   100.2K     20.3ms     25.3ms
4000 →    85.5K     47.9ms     59.8ms
```

- 500→2000：QPS 上升 13%，但延迟上升 3.6x。更多连接引入更多 epoll 事件，每条连接分到的处理时间减少
- 2000→4000：QPS 反而下降 15%，延迟再翻倍。**瓶颈不在连接数，在 epoll 事件切换开销**
- 4000 连接时 p999 达 805ms，说明极端情况下有连接在 epoll 队列中排队很久

### 2. 消息大小的影响

```
消息       500conns      1000conns
64B  →   88.6K (5.6ms)  100.2K (20.3ms)
256B →   99.5K (10.2ms)  - 
1024B→   93.7K (5.3ms)   -
```

- 256B 消息 QPS 反超 64B 消息（99.5K vs 88.6K），说明 epoll 事件处理才是瓶颈，每帧多读一点字节反而提升了吞吐
- 1024B 消息达到 **91.5 MB/s** 带宽，接近单机回环的极限

### 3. 线程数扩展性 (4000 conns)

```
4 threads  → 85,508 QPS
8 threads  → 82,500 QPS  (反而略下降)
```

- **4→8 线程无收益**，瓶颈在单个 epoll 的锁争抢和 kernel socket 队列处理，不是 CPU
- 在 WSL2 虚拟化环境下额外线程甚至带来调度开销

---

## 总结

| 指标 | 最佳值 | 对应配置 |
|------|--------|----------|
| **峰值 QPS** | ~100,000 req/s | 2000 conns / 4 threads / 64B |
| **峰值带宽** | ~91.5 MB/s | 500 conns / 4 threads / 1024B |
| **最低延迟** | ~5.3ms avg | 500 conns / 4 threads / 1024B |
| **最佳平衡** | 100K QPS + 20ms | 2000 conns / 4 threads / 64B |

该自实现 muduo 库在本地回环下的性能接近 muduo 官方库水平。高并发（4000+）下 p999 尾延迟显著上升，未来可在 epoll 事件公平调度方面优化。

---

## 重现命令

```bash
# 编译
cd build && cmake .. && cmake --build . -j$(nproc)

# 启动服务器
LD_LIBRARY_PATH=./lib ./bin/benchserver > /dev/null 2>&1 &

# 运行压测 (可选参数: -c 连接数 -t 线程数 -d 时长 -s 消息大小)
LD_LIBRARY_PATH=./lib ./bin/benchmark -c 2000 -t 4 -d 10 -s 64

# 运行完整套件
./run_bench2.sh
```
